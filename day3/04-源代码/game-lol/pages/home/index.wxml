<view class="list">
  <view class="hero"
  wx:key="id" wx:for="{{heroList}}" bindtap="toDetail" data-id="{{item.id}}">
  <image src="{{item.iconUrl}}"></image>
  <text>{{item.name}}</text>
  </view>
</view>