<view class="ul">
<!-- 对应的每个英雄 -->
  <navigator url="/pages/detail/detail?skill={{item.skill}}" class="li" wx:for="{{heroList}}" wx:key="heroName">
    <image src="{{item.icon}}"></image>
    <text>{{item.heroName}}</text>
  </navigator>
</view>