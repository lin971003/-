<view class="list">
  <navigator url="/pages/detail/detail?skill={{item.skill}}" class="hero" wx:for="{{heroList}}" wx:key="heroName">
    <image src="{{item.icon}}"></image>
    <text>{{item.heroName}}</text>
  </navigator>
</view>