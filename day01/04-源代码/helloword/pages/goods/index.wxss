
page{
  display: flex;
  justify-content: center;
}
.goods {
  width: 234px;
  height: 324px;
  border: 1px solid #ccc;
  display: flex;
  /* 子元素垂直方向上排列,同时如果想让子元素水平居中，这两个方向是交叉的，就使用align-items */
  flex-direction: column;
  align-items: center;
  padding:0 22px;
  box-sizing: border-box;
}

.goods image {
  width: 150px;
  height: 150px;
  margin-top:30px;
}

/* 文字展示在两行，超出给省略号 */
.goods .name {
  overflow: hidden;
  text-overflow: ellipsis;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  margin:44px 0 22px;
}
.goods .price{
  color: #e1251b;
  font-size: 14px;
  /* 给定父元素宽度，子元素默认text-align:left */
  /* width: 100%; */

  /* flex布局时面，父元素规定了子元素按什么方式对齐，在子元素可以通过align-self调整 */
  align-self:flex-start;
}
.price .num{
  font-size: 20px;
}
