<view class="list">
  <view bindtap="toDetail" data-id="{{item.id}}" class="hero" wx:for='{{heroList}}' wx:key='*this'>
    <image src="{{item.iconUrl}}"></image>
    <text>{{item.name}}</text>
  </view>
</view>